import React from 'react'

const validatorContext = React.createContext()

export const Provider = validatorContext.Provider
export const Consumer = validatorContext.Consumer

export default validatorContext